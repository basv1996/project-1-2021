export function getForm() {
    const form = document.querySelector("form");
    const searchValue = document.querySelector('main form input').value;
    form.addEventListener(submit, )
    console.log(searchValue)
}